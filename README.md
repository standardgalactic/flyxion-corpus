# Flyxion TeX Corpus

This working tree was assembled from `inventory.tsv`. Original repository and path provenance is preserved beneath `sources/OWNER/REPOSITORY/`.

Collected files: 967  
Already identical: 0  
Missing from cache: 0  
Conflicts retained: 0  

Compilation has not been attempted. `metadata/root-candidates.tsv` lists likely root documents containing a document class. Modular documents may still require files that did not contain the searched author name and therefore were not part of the original inventory.
